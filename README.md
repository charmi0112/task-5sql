# task-5sql
Aggregate Functions & GROUP BY (SQL)
A sample students table was created
Counts all rows
Includes rows with NULL values
Counts only rows where marks is NOT NULL
Adds all marks
NULL values are ignored automatically
Calculates average marks
NULL values are ignored
Finds lowest and highest marks
Groups students with same age
Counts students in each age group
Shows how many students are in each department
Very common real-world use
How aggregate functions treat NULL
Shows strong conceptual clarity
